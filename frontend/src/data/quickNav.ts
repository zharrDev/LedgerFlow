// Backward-compatible re-export agar consumer lama (HeaderSearchResults)
// tetap memakai path yang sama. Sumber konfigurasi = data/navigation.ts.
export { filterQuickNav } from "./navigation";
export type { QuickNavItem } from "./navigation";
