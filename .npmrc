# Vercel & CI: relaksasi peer-deps agar `npm install`/`npm ci` tidak gagal
# ERESOLVE pada konflik peer frontend (react 19 vs @react-three/fiber).
# node_modules lokal & build terverifikasi dengan kombinasi ini.
legacy-peer-deps=true
